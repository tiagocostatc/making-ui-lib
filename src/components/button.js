import React from 'react';

export const Button = (props) => {

    return (
        <div>
            And some Text
            <button onClick={() => alert("You clicked =)")}>Click me please</button>
        </div>
    )
}